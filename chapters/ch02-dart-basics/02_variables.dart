// Глава 2, «Переменные — коробки для данных».
void main() {
  var name = 'Миша';
  print(name);

  var score = 0;
  print(score);   // 0

  score = 10;
  print(score);   // 10

  score = score + 5;
  print(score);   // 15
}
